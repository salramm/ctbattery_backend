node_modules
dist
.env
.env.*
*.log
.git
# Large data — not needed in the image (import runs against a mounted/host file).
data/usurdb.json
data/usurdb.json.gz
data/territories.geojson
data/*.db
data/*.db-wal
data/*.db-shm
# Keep data/territories-simple.geojson (runtime asset) — do NOT ignore it.

# Eversource Grid Edge raw source — not needed in the backend image (map-only, served as tiles by web)
data/ess/Eversource_grid_map_data/
